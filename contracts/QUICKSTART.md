
# ⚡ Быстрый Старт

## 1️⃣ Установка зависимостей

```bash
cd contracts
npm install
```

## 2️⃣ Настройка переменных окружения

В Replit Secrets (Tools → Secrets 🔒) добавьте:

| Переменная | Значение | Описание |
|-----------|----------|----------|
| `PRIVATE_KEY` | `0x...` | Ваш приватный ключ (66 символов) |
| `POLYGON_RPC_URL` | `https://polygon-rpc.com` | RPC для Mainnet |
| `POLYGON_TESTNET_RPC_URL` | `https://rpc-amoy.polygon.technology` | RPC для Testnet |
| `POLYGONSCAN_API_KEY` | `...` | Для верификации (опционально) |

## 3️⃣ Развертывание контракта

### Testnet (рекомендуется для начала):
```bash
npm run deploy:amoy
```

### Mainnet (для продакшена):
```bash
npm run deploy:polygon
```

**Результат:**
```
✅ ArbitrageExecutor deployed to: 0x1234...5678
```

## 4️⃣ Сохраните адрес контракта

Добавьте в Secrets:
```
ARBITRAGE_CONTRACT=0x1234...5678
```

## 5️⃣ Авторизуйте кошелек

Получите адрес вашего кошелька из приватного ключа:

```bash
# В корне проекта
node -e "console.log(require('ethers').Wallet.fromPhrase(process.env.PRIVATE_KEY).address)"
```

Затем авторизуйте:
```bash
cd contracts
npx tsx scripts/authorize-executor.ts 0x1234...5678 0xYOUR_WALLET_ADDRESS
```

## 6️⃣ Проверьте авторизацию

```bash
npx tsx scripts/check-authorization.ts 0x1234...5678 0xYOUR_WALLET_ADDRESS
```

**Ожидаемый результат:**
```
✅ Executor is authorized to execute trades
```

## 7️⃣ Перезапустите приложение

Нажмите Stop → Run в Replit

## ✅ Готово!

Теперь бот может исполнять реальные арбитражные сделки через Flash Loans.

---

## 🆘 Troubleshooting

### Ошибка: "Insufficient funds"
- Убедитесь, что на кошельке есть MATIC для газа (~0.1 MATIC)

### Ошибка: "Contract not found"
- Проверьте, что контракт развернут: `npx hardhat verify --network amoy <ADDRESS>`

### Ошибка: "Not authorized"
- Выполните шаг 5 для авторизации кошелька

### Ошибка: "Invalid private key"
- Формат должен быть: `0x` + 64 символа (hex)
