
# 🚀 One-Click Setup & Deployment

## Быстрый старт (1 команда)

### Для тестнета Amoy:
```bash
cd contracts && npm run one-click-deploy
```

### Для mainnet Polygon:
```bash
cd contracts && npm run one-click-deploy:polygon
```

## Что происходит автоматически:

1. ✅ Установка всех зависимостей (включая @chainlink/contracts, @openzeppelin/contracts-upgradeable)
2. ✅ Запрос тестовых токенов из faucet (только для testnet)
3. ✅ Компиляция контрактов
4. ✅ Проверка размеров контрактов
5. ✅ Развертывание контракта
6. ✅ Обновление .env файла с адресом контракта
7. ✅ Настройка мониторинга

## Отдельные команды

### Только запрос токенов из faucet:
```bash
npm run faucet
```

### Полная настройка тестнета (faucet + deploy):
```bash
npm run setup-testnet-full
```

### Только компиляция:
```bash
npm run compile
```

### Только развертывание:
```bash
npm run deploy:amoy    # тестнет
npm run deploy:polygon # mainnet
```

## Настройка Tenderly (опционально)

Для включения симуляции сделок:

1. Создайте аккаунт на [tenderly.co](https://tenderly.co)
2. Создайте проект "arbitrage-bot"
3. Добавьте в `.env`:
```env
ENABLE_TENDERLY=true
TENDERLY_API_KEY=your_api_key
TENDERLY_PROJECT_ID=your_account_name
```

## Проверка после развертывания

1. Откройте Settings → Network в UI
2. Проверьте, что отображается адрес контракта
3. Нажмите "Authorize Executor"
4. Запустите бота с Dashboard

## Troubleshooting

### Ошибка: "File not found"
```bash
npm install --legacy-peer-deps
```

### Ошибка: "Insufficient funds"
```bash
npm run faucet
# Подождите получения токенов, затем повторите deployment
```

### Ошибка: "Contract size exceeds limit"
Контракт оптимизирован и должен проходить проверку размера.
Если ошибка сохраняется:
```bash
npm run size  # Проверить текущие размеры
```

## Поддержка

- 📚 Документация: `/contracts/README.md`
- 🔧 Настройки: `/contracts/hardhat.config.ts`
- 💬 Issues: GitHub Issues
