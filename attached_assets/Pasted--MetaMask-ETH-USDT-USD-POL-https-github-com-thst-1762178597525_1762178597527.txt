Создай полный «MetaMask + ETH/USDT/USD-POL → полный торговый офис» в проекте https://github.com/thstt18-ai/cbqnkx1:

1. MetaMask + ETH/USDT/USD-POL → полный торговый офис:
   - тема: MUI dark, иконка: «💰»
   - открывается по кнопке «MetaMask + ETH/USDT/USD-POL» в главном меню
   - вкладки: «Баланс», «Торговля», «Аналитика», «Отчёты», «Поддержка»

2. MetaMask + ETH/USDT/USD-POL:
   - подключается MetaMask кошелёк (ETH, USDT, USD-POL)
   - показывает текущий баланс MetaMask (ETH, USDT, USD-POL)
   - кнопка «Подключить MetaMask» → подключение MetaMask через Web3.js
   - кнопка «Отключить MetaMask» → отключение MetaMask
   - кнопка «Начать торговлю с баланса» → начать торговлю с баланса MetaMask

3. Баланс:
   - показывает текущий баланс MetaMask (ETH, USDT, USD-POL)
   - кнопка «Пополнить баланс» → подключение MetaMask → перевод на баланс
   - кнопка «Вывести баланс» → подключение MetaMask → перевод с баланса

4. Торговля:
   - ввод суммы → выбор стратегии (Grid, TWAP, Momentum, Delta-Neutral)
   - описание: «Grid» vs «TWAP» vs «Momentum» vs «Delta-Neutral»
   - кнопка «Подтвердить» и «Отказаться»
   - при «Подтвердить» → запуск стратегии
   - при «Отказаться» → возврат на баланс

5. Аналитика:
   - метрики: Win-Rate, Sharpe, PnL, equity, gas-spent
   - графики: equity-curve, PnL-curve, gas-curve
   - кнопка «Экспорт в PDF» → экспорт в PDF
   - кнопка «Экспорт в CSV» → экспорт в CSV

6. Отчёты:
   - ежедниевные отчёты (PDF)
   - кнопка «Скачать отчёт» → скачивает PDF-отчёт
   - кнопка «Скачать CSV» → скачивает CSV-отчёт

7. Поддержка:
   - встроенный FAQ
   - встроенный чат (Gemini-ответы)
   - встроенный тикет-система (Gemini-ответы)

8. Без Ledger:
   - только encrypted keystore + Meta-TX (EIP-2771 + EIP-2612)
   - без approve и без MATIC на балансе

9. Без GUI-ошблоков:
    - запускается через встроенный HTTP-сервер (Monaco + WebPreview)
    - без Electron, без X11, без Docker
    - без GUI-ошибок, без HH12, без платных функций

10. README: как запустить и как пользоваться
11. Проверка: `npm run dev` → открывается встроенный редактор, Gemini активен, ошибок нет