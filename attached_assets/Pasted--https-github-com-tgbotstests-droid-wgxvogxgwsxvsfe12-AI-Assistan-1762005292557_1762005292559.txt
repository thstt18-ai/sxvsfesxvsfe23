Создай в репо https://github.com/tgbotstests-droid/wgxvogxgwsxvsfe12
встроенную страницу AI-Assistant:

1. Путь: packages/ai-page/src/App.tsx  
   - единая страница /ai-assistant  
   - MUI dark-theme, responsive, иконка «🧠»

2. Функции страницы:
   - drag-and-drop / click → загрузка любых файлов (zip, json, sol, ts)  
   - кнопка «Скачать с GitHub» → ввод repo-URL → автоматический clone  
   - поле ввода задачи (textarea) + кнопка «Send to AI»  
   - realtime-лог выполнения (timestamp, статус, результат)  
   - история задач (сохраняется в localStorage)  
   - кнопка «Скачать результат» → zip-архив изменённых файлов  
   - кнопка «Push to GitHub» → commit + push (OAuth, без токена)  
   - toggle «Auto-fix on save» → AI наблюдает за файлами и чинит ошибки  
   - progress-bar + статус «idle / running / done / error»

3. Интеграция Gemini:
   - использует @google/gemini-cli (устанавливается при первом запуске)  
   - авторизация через встроенный Google OAuth (без API-ключа)  
   - работает в режиме «watch» на корне проекта

4. Ограничения:
   - без Telegram, без Docker, без env-токенов  
   - без root-доступа к Replit  
   - только браузер + файловая система

5. UX-детали:
   - drag-zone = вся левая колонка  
   - лог = правая колонка, auto-scroll  
   - тема = MUI dark, primaryColor = teal  
   - иконки: FileUpload, CloudDownload, PlayCircle, SaveAlt

6. Безопасность:
   - перед применением patch → показ diff + кнопка «Apply»  
   - перед push → показ commit-message + кнопка «Confirm»

7. Запуск:
   - npm run dev → открывается /ai-assistant  
   - первый запуск → автоматическая установка gemini-cli и авторизация

8. Документация:
   - README.md в packages/ai-page/  
   - скриншоты интерфейса  
   - FAQ: как отключить авто-фикс, как сменить branch

9. Проверка:
   - pnpm install && pnpm dev → страница открывается, файлы загружаются, AI чинит ошибки, результат можно скачать или запушить

10. Готово: полноценная встроенная страница AI-ассистента для будущих доработок проекта